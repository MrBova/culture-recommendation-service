import React from 'react';
import './header.css';

const Header = () => {
    return (
        <div className='header'>
            <button>Логин</button>
            <input />
        </div>
    );
};

export default Header;